// Video 29: Entry point – run with npm run dev or npm run build && npm start
// Ensures dist/ and clean structure; add more files under src/ as needed.

const greeting = 'TypeScript project is set up.';
console.log(greeting);

export {};
