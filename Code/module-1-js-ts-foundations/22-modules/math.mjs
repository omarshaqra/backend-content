// ESM module: export
// Used by: import { add, PI } from './math.mjs'

export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

export const PI = 3.14159;

// Default export (one per module)
// export default { add, subtract, PI };
